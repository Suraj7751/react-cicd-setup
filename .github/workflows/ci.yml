name: "sample react CI pipeline/workflow"

on:
    push:
        branches:
            - main
            - master

jobs:
    build-and-test:
        runs-on: ubuntu-latest

        steps:
           - name: "setup nodejs"
             uses: actions/setup-node@v3
             with:
                node-version: '21' 
            